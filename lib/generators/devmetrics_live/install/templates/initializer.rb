DevmetricsLive.setup do |config|
  # Mount path (informational — change by updating the mount point in routes.rb)
  # config.mount_path = "/devmetrics"

  # Slow query threshold in milliseconds (queries above this are logged)
  # config.slow_query_threshold_ms = 100

  # Maximum number of slow queries stored in the database
  # config.max_slow_queries = 500
end
